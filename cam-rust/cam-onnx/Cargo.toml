[package]
name = "cam-onnx"
version = "0.1.0"
edition = "2021"

[dependencies]
cam-types = { path = "../cam-types" }
log = { workspace = true }
ort = { version = "2.0.0-rc.12", optional = true }

[features]
default = []
ort = ["dep:ort"]
