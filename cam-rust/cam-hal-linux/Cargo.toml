[package]
name = "cam-hal-linux"
version = "0.1.0"
edition = "2021"

[dependencies]
cam-types = { path = "../cam-types" }
cam-isp = { path = "../cam-isp" }
cam-hal = { path = "../cam-hal" }
cam-core = { path = "../cam-core" }
log = { workspace = true }
thiserror = { workspace = true }

# V4L2 via rscam crate
rscam = { version = "0.5", optional = true }

[features]
default = []
v4l2 = ["rscam"]
