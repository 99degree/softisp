[package]
name = "cam-hal-android"
version = "0.1.0"
edition = "2021"

[dependencies]
cam-types = { path = "../cam-types" }
cam-hal = { path = "../cam-hal" }
log = { workspace = true }
thiserror = { workspace = true }
ndk-sys = "0.3"
ndk = { version = "0.6", features = ["hardware_buffer"] }